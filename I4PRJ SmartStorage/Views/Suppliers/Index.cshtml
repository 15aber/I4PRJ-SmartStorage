@model I4PRJ_SmartStorage.ViewModels.SupplierViewModel

@{
    ViewBag.Title = "Suppliers";
}

<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <h5>List of Suppliers</h5>
                    <div class="ibox-tools">
                        @Html.ActionLink("Create New Supplier", "Create", null, new { @class = "btn btn-primary" })
                        @Html.ActionLink("Create New Supplier Report", "CreateNewReport", null, new { @class = "btn btn-primary" })
                    </div>
                </div>
                <div class="ibox-content">
                    <table id="suppliers" class="table table-striped table-bordered table-hover">
                        <thead>
                            <tr>
                                <th>
                                    @Html.LabelFor(model => model.Supplier.Name)
                                </th>
                                <th>
                                    @Html.LabelFor(model => model.Supplier.Updated)
                                </th>
                                <th>
                                    @Html.LabelFor(model => model.Supplier.ByUser)
                                </th>
                                <th>Options</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                        <tfoot>
                        </tfoot>
                    </table>

                </div>
            </div>
        </div>
    </div>
</div>

@section scripts {
    <script type="text/javascript" src="~/Scripts/site/datatables/suppliersIndex.js"></script>
}